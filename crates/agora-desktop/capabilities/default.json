{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs for the bundled connect page, plus the opener so its website link opens in the system browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
